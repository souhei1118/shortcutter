<!--id="comments_areaにをrenderで指定した部分テンプレートに更新-->
$("#comments_area").html("<%= j(render 'public/comments/review', shortcut: @shortcut, comment: @comment) %>");
<!--フォームを送信したあとにフォームの値を空にする-->
$("textarea").val('');
$("hidden_field").val('');